/**
 * Shared components — sorted by role so they stay easy to find.
 *
 * - ui/        Primitives: Button, Input, Select, Modal, Table, …
 * - feedback/  Status UI: LoadingState, EmptyState, ErrorBanner, …
 * - layout/    Shell pieces: AppShell, Sidebar, PageHeader, …
 * - forms/     Shared form helpers used across multiple pages
 *
 * Page-only UI belongs under pages/<page>/components/ — not here.
 * Promote to this folder only when the same piece is reused ~2–3 times.
 */
export {};
